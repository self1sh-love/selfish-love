# 羽毛球视频对比分析系统 GUI 使用指南

专业的视频对比分析GUI，支持原视频与处理结果的实时对比、参数调整、配置保存等功能。

## 🎯 核心功能

- ✅ **三屏对比显示** - 原始视频、处理结果、姿态分析信息并排显示
- ✅ **参数可视化调整** - 通过UI界面调整所有处理参数
- ✅ **配置保存/加载** - 保存常用配置，快速切换设置
- ✅ **实时进度反馈** - 显示处理进度、FPS、处理状态
- ✅ **颜色自定义** - 自定义头部、手臂、躯干、腿部的显示颜色
- ✅ **GPU加速支持** - 一键启用/禁用GPU和FP16加速

## 🚀 快速开始

### 第一步：确保GPU已配置（如需加速）

```batch
# 如果还未配置GPU，先运行修复脚本
scripts\fix_gpu.bat

# 验证GPU
scripts\test_gpu.bat
```

### 第二步：启动GUI

```batch
# 方式1：双击运行
run_compare_gui.bat

# 方式2：命令行运行
venv\Scripts\activate
python pose_detection_compare_gui.py
```

## 📖 使用指南

### 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  文件选择区域                                                  │
│  [选择输入视频] [选择输出路径]                                  │
├─────────────────────────────────────────────────────────────┤
│  视频对比区域（3个面板）                                        │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐                │
│  │ 原始视频   │  │ 处理结果   │  │ 姿态分析   │                │
│  │           │  │           │  │           │                │
│  └───────────┘  └───────────┘  └───────────┘                │
│  [▶ 播放] [⏸ 暂停] [⏮ 重置]  ━━━━━━━━━━━  0/0              │
├─────────────────────────────────────────────────────────────┤
│  参数设置区域（标签页）                                         │
│  ┌─ 模型设置 ─ 显示设置 ─ 颜色设置 ─ 分析设置 ─┐              │
│  │ [各种参数调整控件]                           │              │
│  └─────────────────────────────────────────────┘              │
│  [开始处理] [停止处理] [保存配置] [加载配置] [重置配置]         │
│  ┌─ 处理进度 ─────────────────────────────────┐              │
│  │ [实时日志和进度信息]                          │              │
│  └─────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

### 基本工作流程

#### 1. 选择视频文件

1. 点击"选择输入视频"按钮
2. 选择要处理的羽毛球训练视频
3. 点击"选择输出路径"指定输出文件位置

#### 2. 调整处理参数

**模型设置标签页**：
- **姿态模型**：选择YOLOv8模型（n/s/m/l）
  - `yolov8n-pose.pt` - 最快，适合实时处理（推荐）
  - `yolov8s-pose.pt` - 平衡速度和精度
  - `yolov8m-pose.pt` - 高精度，速度较慢
  - `yolov8l-pose.pt` - 最高精度，速度最慢
- **置信度阈值**：0.0-1.0，控制检测灵敏度（默认0.25）
- **使用GPU**：启用GPU加速（RTX 4090推荐启用）
- **使用FP16**：启用半精度加速（显存优化）

**显示设置标签页**：
- **线条宽度**：骨架线条粗细（1-10）
- **关键点大小**：关键点圆圈大小（1-20）
- **显示标签**：显示关键点编号（调试用）
- **慢动作因子**：1.0=原速，0.5=2倍慢放，0.25=4倍慢放
- **视频质量**：输出视频质量（0-100）

**颜色设置标签页**：
- **头部颜色**：鼻子、眼睛、耳朵（默认红色）
- **手臂颜色**：肩膀、肘部、手腕（默认绿色）
- **躯干颜色**：肩膀到臀部连接（默认蓝色）
- **腿部颜色**：臀部、膝盖、脚踝（默认黄色）

**分析设置标签页**：
- **启用AI分析**：自动分析姿势问题
- **显示角度**：显示关节角度数据
- **显示问题**：显示检测到的姿势问题
- **保存关键帧**：自动保存重要瞬间

#### 3. 开始处理

1. 点击"开始处理"按钮
2. 观察"处理进度"区域的实时日志
3. 处理完成后会自动加载对比视频
4. 使用播放控制按钮查看结果

#### 4. 查看对比结果

- **左侧面板**：原始视频，未经处理
- **中间面板**：处理结果，带姿态骨架标注
- **右侧面板**：姿态分析数据
  - 关节角度（绿色=正常，红色=有问题）
  - 检测到的姿势问题
  - 改进建议

## 💡 使用场景

### 场景1：快速技术诊断

**需求**：快速查看学员的动作问题

**设置**：
- 模型：yolov8n-pose.pt（快速）
- GPU：启用
- FP16：启用
- AI分析：启用
- 慢动作：1.0（原速）

**步骤**：
1. 选择视频 → 开始处理 → 5秒内完成
2. 查看右侧分析面板的问题提示
3. 使用播放控制精确定位问题帧

### 场景2：详细动作分析

**需求**：细致分析专业动作技术

**设置**：
- 模型：yolov8m-pose.pt（高精度）
- 慢动作：0.25（4倍慢放）
- 显示角度：启用
- 保存关键帧：启用

**步骤**：
1. 处理视频（可能需要1-2分钟）
2. 使用慢放查看细节
3. 在进度条上精确定位关键瞬间
4. 对比分析原视频和标注结果

### 场景3：教学课件制作

**需求**：制作带姿态标注的教学视频

**设置**：
- 模型：yolov8s-pose.pt（平衡）
- 视频质量：90（高质量）
- 颜色设置：调整为教学友好的配色
- 保存关键帧：启用

**步骤**：
1. 调整颜色使其清晰可见
2. 处理视频生成标注版本
3. 使用对比功能验证质量
4. 输出文件可直接用于教学

## 🔧 配置管理

### 保存配置

1. 调整好所有参数
2. 点击"保存配置"按钮
3. 配置保存到 `compare_config.json`

### 加载配置

1. 点击"加载配置"按钮
2. 自动恢复上次保存的设置

### 重置配置

1. 点击"重置配置"按钮
2. 恢复所有默认值

### 配置文件格式

配置保存为JSON格式，可手动编辑：

```json
{
  "pose_model": "yolov8n-pose.pt",
  "confidence": 0.25,
  "use_gpu": true,
  "use_fp16": true,
  "line_width": 2,
  "keypoint_size": 4,
  "show_labels": false,
  "color_head": [255, 0, 0, 255],
  "color_arms": [0, 255, 0, 255],
  "color_torso": [0, 0, 255, 255],
  "color_legs": [255, 255, 0, 255],
  "enable_analysis": true,
  "show_angles": true,
  "show_issues": true,
  "slowmo_factor": 1.0,
  "save_keyframes": false,
  "video_quality": 90
}
```

## 📊 性能优化建议

### RTX 4090 最佳配置

```
✅ 使用GPU: 启用
✅ 使用FP16: 启用
✅ 模型: yolov8n-pose.pt
✅ 预期速度: 100-150 FPS
✅ 处理1分钟视频: 约5秒
```

### 低配置电脑

```
⚙️ 使用GPU: 禁用
⚙️ 模型: yolov8n-pose.pt
⚙️ 预期速度: 10-20 FPS
⚙️ 处理1分钟视频: 3-6分钟
```

### 高精度分析

```
🎯 模型: yolov8m-pose.pt
🎯 置信度: 0.3-0.4
🎯 牺牲: 处理速度降低30-50%
🎯 收益: 关键点精度提升15-20%
```

## 🆘 常见问题

### Q1: 处理速度很慢（<20 FPS）

**A:** GPU未启用或未正确配置

**解决方案**：
1. 运行 `scripts\fix_gpu.bat`
2. 验证 `scripts\test_gpu.bat` 显示CUDA可用
3. 在GUI中确认"使用GPU"已勾选

### Q2: 姿态检测不准确

**A:** 模型太小或置信度阈值不合适

**解决方案**：
1. 尝试更大的模型（m或l）
2. 调整置信度阈值（0.2-0.4）
3. 确保视频中人物清晰可见

### Q3: 显存不足错误

**A:** 模型太大或FP16未启用

**解决方案**：
1. 启用FP16加速
2. 使用更小的模型（n或s）
3. 降低视频分辨率

### Q4: 处理后的视频没有声音

**A:** 需要安装ffmpeg或moviepy来复制音频

**解决方案**：
```bash
# 推荐：安装ffmpeg（最快）
choco install ffmpeg

# 或使用Python库
pip install moviepy
```

查看日志中的音频处理信息：
- `✓ 音频复制完成` - 成功
- `⚠ 音频复制失败` - 需要安装工具

### Q5: 处理后的视频无法播放

**A:** 编码器问题或文件损坏

**解决方案**：
1. 检查输出路径是否有写入权限
2. 确保有足够的磁盘空间
3. 尝试使用VLC播放器

### Q6: GUI界面显示异常

**A:** 分辨率或DPI缩放问题

**解决方案**：
1. 调整窗口大小
2. 检查Windows显示缩放设置
3. 重启GUI应用

### Q7: 处理过程中看不到实时画面

**A:** 确保在处理标签页中观察，而不是在播放控制

**说明**：
- 处理过程中，三个画布会每10帧自动更新
- 左侧显示原始帧，中间显示处理帧，右侧显示分析
- 处理完成后，点击播放按钮查看完整视频

## 🎨 颜色配置建议

### 教学友好配色

```
头部: 红色   (255, 0, 0)
手臂: 绿色   (0, 255, 0)
躯干: 蓝色   (0, 0, 255)
腿部: 黄色   (255, 255, 0)
```

### 高对比度配色（投影仪）

```
头部: 品红   (255, 0, 255)
手臂: 青色   (0, 255, 255)
躯干: 白色   (255, 255, 255)
腿部: 橙色   (255, 165, 0)
```

### 柔和配色（长时间观看）

```
头部: 浅红   (255, 100, 100)
手臂: 浅绿   (100, 255, 100)
躯干: 浅蓝   (100, 100, 255)
腿部: 浅黄   (255, 255, 100)
```

## 📝 键盘快捷键（计划中）

当前版本通过按钮控制，未来版本将支持：

- `Space` - 播放/暂停
- `←` / `→` - 后退/前进一帧
- `Home` - 跳到开始
- `End` - 跳到结束
- `Ctrl+O` - 打开视频
- `Ctrl+S` - 保存配置
- `Ctrl+P` - 开始处理

## 🔄 更新日志

### v1.1 (2024-11) - 问题修复版
- 🔧 **修复**：视频输出添加音频支持（ffmpeg/moviepy）
- 🔧 **修复**：姿态标注颜色显示问题（RGB→BGR转换）
- 🔧 **修复**：处理过程中实时预览显示
- 🔧 **优化**：每10帧更新一次预览，提升用户体验
- 🔧 **增强**：详细的处理日志和错误提示

### v1.0 (2024-11) - 初始版本
- ✅ 三屏对比显示
- ✅ 参数化UI调整
- ✅ 配置保存/加载
- ✅ GPU加速集成
- ✅ 实时进度反馈
- ✅ 颜色自定义
- ✅ AI姿态分析集成

## 📞 技术支持

遇到问题？

1. 查看处理进度区域的错误信息
2. 运行 `scripts\check_nvidia.bat` 诊断环境
3. 查看 `README.md` 了解更多功能
4. 查看 `TRAINING_GUIDE.md` 了解详细使用

---

**快速开始：**
1. 运行 `run_compare_gui.bat`
2. 选择输入视频和输出路径
3. 调整参数（可选）
4. 点击"开始处理"
5. 观看对比结果
